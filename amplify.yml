version: 1
frontend:
  phases:
    build:
      commands: []
  artifacts:
    baseDirectory: /frontend
    files:
      - '**/*'
  cache:
    paths: []
backend:
  phases:
    build:
      commands:
        - amplifyPush --simple
